# Sample seed (not loaded)

This folder holds the original demo dataset. The live dashboard reads only
`content/{engagements,findings,...}` — restore files from here if you want the
examples back, then set `"sampleData": true` in `content/org.json`.
